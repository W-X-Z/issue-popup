
  # Convert to HTML code

  This is a code bundle for Convert to HTML code. The original project is available at https://www.figma.com/design/91BZw9zlX7vOrpwNdOwPwV/Convert-to-HTML-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  